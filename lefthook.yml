pre-commit:
  scripts:
    "format-files.bash":
      runner: bash
  commands:
    unit-tests:
      run: "./dev/bin/testall"
commit-msg:
  commands:
    commitlint:
      run: "commitlint --edit={1}"
